# 增长智能体边界

Growth Copilot 采用“事实快照 → 诊断 → 建议草稿 → 商家审批 → 执行 → 复盘”链路。每条 Insight 必须提供证据、时间范围、样本量和置信等级；证据不足时不生成伪因果结论。

当前领域契约只允许创建 `DRAFT` 计划，审批后才变为 `APPROVED`。它不会直接支付、退款、修改库存、发送营销消息或发布内容。后续服务端需要将批准动作写入任务中心，并由已有第二阶段创作工作流执行。

