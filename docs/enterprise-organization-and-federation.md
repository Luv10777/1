# 企业组织与联邦边界

Phase 5 使用通用 `OrganizationNode` 树，不把总部/区域/门店写死为固定层级。品牌、合法经营主体和门店是独立实体。独立法人或数据权属不同的加盟商默认使用独立 Tenant，通过显式 Federation Scope 授权总部查看允许的汇总或明细。

当前实现为本地 Mock：组织节点、策略继承、RBAC+ABAC 访问判断均有契约测试；没有服务端组织 API、联邦协议、审计导出或真实企业目录。
