# P1-01 项目审计

- 项目目录此前为空，未发现已有应用、路由或环境文件。
- 本阶段采用 Vue 3 + Vite + Vue Router，浏览器端默认 Mock 认证。
- 第一阶段目标是可访问、可演示、可扩展的前端工作台，不接入真实模型、数据库或第三方密钥。

## 边界

前端包含登录、路由守卫、控制台、占位路由、403/404 和响应式布局；FluAPI/ToAPIs 实际调用、Spring Boot、RabbitMQ、Redis、媒体 Worker、真实商家数据留到后续阶段。
