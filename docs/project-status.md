# 项目状态

> 更新时间：2026-08-25 · 版本基线：`f43dddd`

## 总体状态：🟡 可演示，生产化进行中

第一阶段前端和第二阶段 M0–M5 首批领域契约已经完成，本地创作工作区可运行，核心 Mock 链路有自动化测试覆盖。由于 Core API、数据库、队列、真实 Provider、服务端鉴权和生产平台 OAuth 尚未接入，项目当前不能作为生产发布系统使用。

## 关键指标

| 指标 | 当前值 | 说明 |
| --- | --- | --- |
| 阶段完成度 | M0–M5 首批完成 | M6 生产化基础未开始 |
| 自动化测试 | 18 / 18 通过 | 覆盖领域契约和 Mock Provider |
| 代码质量 | lint 通过 | `npm run lint -- --quiet` |
| 构建状态 | 通过 | `npm run build` |
| 可访问工作区 | `/creative` | 本地 `http://localhost:4173/creative` |

## 已完成工作

第一阶段已提供登录、控制台、稳定路由、权限前置模型和响应式布局。第二阶段已提供工作流状态机、编排器、Provider 契约、创作编译器、事实阻断、Prompt/媒体/视频 QA、审核契约和平台连接器契约。

## 当前风险与缓解措施

| 风险 | 影响 | 缓解措施 | 负责人 |
| --- | --- | --- | --- |
| Mock 被误认为生产能力 | 可能误发布或错误估算交付范围 | README、记忆和阶段文档显式标注 Mock 边界 | 项目协作者 |
| 没有服务端持久化和 Worker | 任务重启后无法恢复，无法保证消费幂等 | M6 先建设 Core API、数据库和可恢复 Worker | 后端协作者 |
| 真实平台凭证和 OAuth 未接入 | 只能导出，不能自动发布或同步评论 | 默认 `EXPORT_ONLY`，授权后逐个平台验收 | 平台接入协作者 |
| Webhook 安全尚未落地 | 回调伪造或重放风险 | 服务端实现签名、时间窗、nonce 和重放存储 | 安全/后端协作者 |
| 视频和数字人链路未生产化 | 只能演示 brief 和 Mock 任务 | 先完成真实 Provider 合同，再做 FFmpeg、TTS 和授权验收 | 媒体协作者 |

## 下一阶段出口条件

M6 不以“页面能展示”为完成标准。必须具备服务端租户/RBAC 校验、数据库迁移、队列恢复和消费幂等、真实对象存储签名上传、Webhook 安全、端到端契约测试及恢复演练，才可进入真实 Provider 和平台发布验收。

## 本地验证命令

```bash
npm test
npm run lint -- --quiet
npm run build
```
